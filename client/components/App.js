import React from 'react';
import SearchBar from './SearchBar';
import '../styles/Main.scss';

const App = (props) => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

App.propTypes = {};

export default App;
